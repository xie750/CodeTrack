#### 2026/07/27  改动

1. 实现左侧边栏收缩功能（改动了App.tsx和styles.tsx这两个文件）

#### 2026/07/31  改动

1. AI 底座抽取（第 0 步）：新增 `backend/app/ai/` 包（`errors` / `schemas` /
   `llm_client` / `guardrails` / `run_recorder`），`model_gateway.py` 的传输层
   搬到通用客户端，模型失败不再静默 —— 每次调用在 `agent_runs` / `agent_steps`
   留记录，失败带 `error_code`。学生侧行为不变（仍降级到规则兜底）。
2. 迁移 `20260731_0005`：`agent_runs` / `agent_steps` 两张表、
   `tasks.hint_forbidden_fragments` 列、`diagnoses.submission_version_id` 唯一索引。
3. 修 commit 边界：LLM 调用不再夹在事务中间，测试结果先提交释放 SQLite 写锁。
4. 本轮**有意没修**的 4 个问题 + 2 个验证环境问题，记在 [已知问题.md](已知问题.md)。

