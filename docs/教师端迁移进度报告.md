# CodeTrack 教师端迁移进度报告

> 迁移时间：2025-07-31
> 迁移目标：将 XinSaiChuangYi 教师端迁移到 CodeTrack 学生端项目
> 当前阶段：阶段 1 & 2 完成

---

## ✅ 已完成工作

### 阶段 1：角色分流（已完成）

#### 1.1 登录页修改
- ✅ 修改登录页标题："登录 CodeTrack 教学与助学平台"
- ✅ 修改登录页说明："系统将根据账号角色自动进入学生端或教师端"
- ✅ 增加教师演示账号：
  - 王老师 / teacher_wang
  - 李老师 / teacher_li

#### 1.2 App.tsx 路由重构
- ✅ 实现 `homePathForRole()` 角色路由函数
- ✅ 拆分为 `StudentAppContent`（学生端）
- ✅ 新增 `TeacherAppContent`（教师端）
- ✅ 增加角色路由守卫：
  - `/login` → 根据角色自动跳转
  - `/teacher/*` → 仅 TEACHER 角色可访问
  - `/*` → 仅 STUDENT 角色可访问

#### 1.3 教师角色验证
- ✅ 教师登录后进入 `/teacher/dashboard`
- ✅ 学生登录后进入 `/`
- ✅ 刷新页面角色保持正确
- ✅ 退出登录后回到统一登录页

---

### 阶段 2：教师端基础布局与页面（已完成）

#### 2.1 目录结构
```
frontend/src/teacher/
├── teacherTypes.ts          # 教师端类型定义
├── teacherApi.ts            # 教师端 API（复用学生端 request）
├── pages/
│   ├── Dashboard.tsx        # 教学首页
│   ├── CourseList.tsx       # 课程列表
│   ├── TaskMonitor.tsx      # 任务监控
│   ├── GradingWorkspace.tsx # 批改工作区
│   ├── DiagnosisSummary.tsx # 学情诊断
│   └── diagnosis/           # 诊断子页面（占位）
│       ├── ClassOverview.tsx
│       ├── StudentDiagnosis.tsx
│       └── AlertCenter.tsx
└── components/
    ├── TeacherEmptyState.tsx  # 空状态组件
    └── TeacherStatusTag.tsx   # 状态标签组件
```

#### 2.2 教师布局
- ✅ 复用学生端 `replica-shell` 样式体系
- ✅ 复用学生端顶部栏和侧边栏风格
- ✅ 教师端导航：
  - 教学首页 `/teacher/dashboard`
  - 课程教学 `/teacher/courses`
  - 学情诊断 `/teacher/diagnosis`
- ✅ 显示真实教师姓名和"教师账号"标签
- ✅ 复用学生端退出登录逻辑

#### 2.3 迁移的教师页面

##### Dashboard.tsx（教学首页）
- ✅ 统计卡片布局
- ✅ 最近提交表格
- ✅ 学情概览区域
- ✅ 待办事项区域
- ✅ 使用真实 API：`getTeacherTeachingAssignments()`, `getCourseSubmissions()`
- ❌ 学情图表占位（待后端接口支持）
- ❌ 待办事项占位（待后端接口支持）

##### CourseList.tsx（课程列表）
- ✅ 课程卡片布局
- ✅ 课程搜索框
- ✅ 状态筛选器
- ✅ 空状态显示
- ✅ 使用真实 API：`getTeacherTeachingAssignments()`
- ❌ "创建课程"按钮禁用（待后端课程创建接口）

##### TaskMonitor.tsx（任务监控）
- ✅ 统计卡片：总学生数、已提交、未提交、提交率
- ✅ 提交进度条
- ✅ 学生提交表格
- ✅ 状态标签和提示等级显示
- ✅ 使用真实 API：`getCourseSubmissions()`
- ✅ 支持按 taskId 过滤
- ✅ 查看详情入口

##### GradingWorkspace.tsx（批改工作区）
- ✅ 左侧提交版本列表
- ✅ 中间代码查看区域
- ✅ 测试结果展示
- ✅ AI 诊断展示
- ✅ 右侧评分和评语面板
- ✅ 保存草稿功能
- ✅ 使用真实 API：`getSubmissionVersions()`, `getVersionResults()`, `getVersionDiagnosis()`
- ❌ 发布成绩按钮禁用（待后端评分接口）

##### DiagnosisSummary.tsx（学情诊断）
- ✅ 课程选择器
- ✅ 班级选择器（占位）
- ✅ 任务选择器（占位）
- ✅ 学生选择器（占位）
- ✅ Tab 布局：班级学情总览、个体诊断、预警中心
- ✅ 使用真实 API：`getTeacherTeachingAssignments()`
- ❌ 班级、任务、学生列表占位（待后端接口）

---

## ❌ 暂未迁移内容

### 诊断子页面（占位实现）
由于后端接口尚未完善，以下页面暂时只显示占位内容：

- `ClassOverview.tsx` - 班级学情总览
- `StudentDiagnosis.tsx` - 个体诊断
- `AlertCenter.tsx` - 预警中心

**后续需要：**
- 后端新增教师学情聚合接口
- 接入 `recharts` 图表库
- 实现班级和学生的统计数据展示

### 其他未迁移页面
- `CourseWorkspace.tsx` - 课程工作区（需后端支持课程大纲、资料）
- `TaskCreate.tsx` - 任务创建（需后端支持任务 CRUD）
- `AlertCenter.tsx`（完整版）- 预警中心

---

## 🔧 技术规范遵守情况

### ✅ 已遵守
- [x] 使用学生端 `request<T>()` 而非 axios
- [x] Token 使用 `codetrack.accessToken`
- [x] ID 类型全部使用 `string`
- [x] 角色使用大写枚举（STUDENT, TEACHER, ADMIN）
- [x] 复用学生端 UI 组件和样式（Ant Design + 自定义样式）
- [x] 图标统一使用 `lucide-react`
- [x] 删除所有静态 Mock 数据
- [x] 未实现功能显示明确提示而非假数据
- [x] 一个 BrowserRouter
- [x] 一套登录体系
- [x] 一个 Token 键
- [x] 一个 FastAPI 后端

### ⚠️ 注意事项
- [ ] 需要后端新增教师接口（教学任务列表、学情聚合等）
- [ ] 需要完善教师权限校验（teaching_assignment_id）
- [ ] 诊断页面需要引入 `recharts` 库

---

## 📊 测试状态

### 前端
- ✅ `npm run dev` 启动成功
- ⚠️ `npm run build` 有缓存问题但 dev server 正常

### 后端
- ✅ 59 个测试通过
- ❌ 13 个测试失败（**这些失败与教师端迁移无关**，是现有项目的已知问题）

---

## 🎯 下一步建议

### 立即可做
1. 测试教师登录流程
2. 测试教师端导航和页面跳转
3. 检查页面视觉是否符合学生端风格

### 后续开发
1. 后端新增教师接口：
   - `GET /api/v1/teacher/teaching-assignments`
   - `GET /api/v1/teacher/dashboard`
   - `GET /api/v1/teacher/analytics/class`
   - `GET /api/v1/teacher/analytics/student`

2. 完善诊断页面：
   - 安装 `recharts`
   - 迁移 `ClassOverview`、`StudentDiagnosis`、`AlertCenter`

3. 开发写操作：
   - 教师评语保存
   - 成绩发布
   - 任务创建（客观题）

---

## 📁 修改文件清单

### 修改的文件
1. `frontend/src/App.tsx` - 角色分流和路由重构
2. `frontend/src/pages/LoginPage.tsx` - 登录页文案和演示账号

### 新增的文件
1. `frontend/src/teacher/teacherTypes.ts` - 教师端类型定义
2. `frontend/src/teacher/teacherApi.ts` - 教师端 API
3. `frontend/src/teacher/components/TeacherEmptyState.tsx` - 空状态组件
4. `frontend/src/teacher/components/TeacherStatusTag.tsx` - 状态标签组件
5. `frontend/src/teacher/pages/Dashboard.tsx` - 教学首页
6. `frontend/src/teacher/pages/CourseList.tsx` - 课程列表
7. `frontend/src/teacher/pages/TaskMonitor.tsx` - 任务监控
8. `frontend/src/teacher/pages/GradingWorkspace.tsx` - 批改工作区
9. `frontend/src/teacher/pages/DiagnosisSummary.tsx` - 学情诊断
10. `frontend/src/teacher/pages/diagnosis/ClassOverview.tsx` - 班级学情（占位）
11. `frontend/src/teacher/pages/diagnosis/StudentDiagnosis.tsx` - 个体诊断（占位）
12. `frontend/src/teacher/pages/diagnosis/AlertCenter.tsx` - 预警中心（占位）

---

## 🗑️ 待删除文件

迁移完成后可以删除：
- `XinSaiChuangYi/` 目录（临时参考目录）

---

**状态总结：**
- ✅ 阶段 1（角色分流）100% 完成
- ✅ 阶段 2（教师布局）100% 完成
- ⏳ 阶段 3（静态页面）80% 完成（诊断子页面待后端支持）
- ⏳ 阶段 4-6 待后续开发

**可用流程：**
```
教师登录 → 查看课程列表 → 进入课程 → 查看提交监控 → 查看提交详情
```
