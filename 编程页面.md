# 编程页面 - 修改清单

> 在现有编程工作台（`/workspace/:taskId`）基础上做局部调整，不动整体三栏布局。

---

## 一、左侧面板：题目描述 → Tab 切换

当前左侧面板只展示题目描述。需改为两个 Tab 切换：

```
修改前：                      修改后：
┌──────────────┐             ┌──────────────────────┐
│ 题目描述      │             │ 题目描述  │ 提交记录    │  ← 新增 Tab 栏
│              │             ├──────────────────────│
│ ...          │             │ 当前选中的 Tab 内容    │
│              │             │ ...                  │
└──────────────┘             └──────────────────────┘
```

### 具体步骤
1. 左侧面板顶部新增 Tab 栏，两个 Tab：**题目描述**（默认选中）、**提交记录**
2. 「题目描述」Tab 内容不变，即原来的题目详情
3. 「提交记录」Tab 展示历史提交列表（表格：提交时间、状态、通过用例数、耗时），点击某条可展开查看详情

---

## 二、右侧面板：测试结果 → 测试用例 / 测试结果 Tab 切换

当前右侧面板展示运行输出和测试结果。需改为两个 Tab，并新增放大/折叠按钮：

```
修改前：                      修改后：
┌──────────────┐             ┌──────────────────────────┐
│ 运行输出      │             │ 测试用例  │ 测试结果        │ ← Tab + [⛶][−]
│ 测试结果      │             ├──────────────────────────│
│              │             │                          │
│ ...          │             │ 当前 Tab 内容              │
│              │             │                          │
└──────────────┘             └──────────────────────────┘
```

### 2.1 测试用例 Tab

```
┌──────────────────────────┐
│ Case 1 │ Case 2 │  [+]   │  ← 用例切换标签，[+] 克隆当前用例
├──────────────────────────┤
│                          │
│ 参数1： [  输入框  ]      │  ← 每行一个参数，预填默认值，可编辑
│ 参数2： [  输入框  ]      │
│                          │
│        [执行代码]          │
└──────────────────────────┘
```

- **Case 标签**：横向排列，当前选中高亮（蓝色边框），点击切换
- **[+] 按钮**：克隆当前选中的用例，新增一个 Case，参数值从当前用例复制
- **参数行**：`参数名：输入框`，预填后端返回的公开测试用例默认值，用户可手动修改
- **[执行代码]**：用当前用例运行代码，运行后自动切到「测试结果」Tab

### 2.2 测试结果 Tab

运行后展示结果表格：

| 用例 | 输入 | 期望输出 | 实际输出 | 结果 |
|------|------|----------|----------|------|
| 1 | [1,2] | [1] | [1] | ✓ |
| 2 | [1] | [] | [] | ✓ |
| ... | ... | ... | ... | ✗ |

底部显示：`通过 4/5  |  耗时 12ms  |  内存 7.2MB`

### 2.3 放大 / 折叠

右侧面板右上角放两个小按钮：
- **[⛶]**：面板放大撑满屏幕
- **[−]**：面板折叠收起（编辑器区域自动扩展）

---

## 三、删除项

以下三个底部面板直接删除：

- ❌ 能力成长
- ❌ 学习建议
- ❌ 错因分析

对应 DOM 和 CSS 一并移除。

---

## 四、交互说明

| 操作 | 行为 |
|------|------|
| 点击「提交记录」Tab | 切换展示历史提交列表 |
| 点击「测试用例」Tab | 切换展示用例编辑区 |
| 点击右侧 [执行代码] | 运行 → 自动切到「测试结果」Tab 展示结果 |
| 点击 Case 标签 | 切换当前用例，更新参数输入框 |
| 点击 [+] | 克隆当前用例新增一个 Case |
| 修改参数后点 [执行代码] | 用自定义参数值运行 |
| 点中间 [提交] | 全量用例提交，结果展示后可手动切到「提交记录」Tab |
