# 原型与业务冲突处理

| 文档差异 | 最终实现 |
|---|---|
| Word 的“5.1 创建班级流程”截图实际是“创建课程” | 按截图实现三步创建课程；教学班创建留在班级管理。 |
| 教师不能创建行政班，但原型有“新建班级” | 新建的是课程内教学班 `ClassGroup`，不修改行政班和学籍。 |
| Word 包含科研入口，开发方案将科研列为后续阶段 | 保留原型入口并明确禁用，不伪装为完成能力。 |
| 原型任务状态与开发文档名称不同 | 后端统一为 `draft -> scheduled -> published -> closed`。 |
| Word 提议 AI 自动生成任务、资料和图谱 | AI 结果只进入草稿或候选状态，教师确认后才能发布。 |
| 隐藏测试教师可见、学生不可见 | 学生任务接口过滤隐藏测试内容，教师提交详情保留完整结果。 |
| AI 预评分与最终成绩容易混淆 | 自动评测、AI 诊断、教师成绩分别持久化；只有 `grade_published` 对学生公开。 |

## 当前真实能力

- SQLite 持久化，不使用页面内模拟数组作为事实源。
- 教师和学生共用课程、班级、任务、提交、评测和成绩表。
- 文件保存到 `backend/uploads/`，下载时按角色和可见性校验。
- 所有关键写操作记录到 `audit_logs`。
- 教师接口校验角色和课程归属。
- 学生接口校验教学班成员关系。

## 生产化剩余外部依赖

- 学校 SSO/JWT 身份提供方。
- 隔离的 C++ 沙箱执行服务；当前评测器使用确定性规则实现接口闭环。
- 真实大模型网关；当前诊断器使用确定性教学规则，审核契约已完整。
- 对象存储和病毒扫描；当前文件保存在本机磁盘。

这些依赖不影响当前本地完整闭环，但部署到公网前必须替换为生产服务。
