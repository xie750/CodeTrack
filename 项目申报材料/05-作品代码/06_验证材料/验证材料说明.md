# 验证材料说明

## 已完成验证

| 验证项 | 结果 | 说明 |
| --- | --- | --- |
| 模型文件完整性 | 通过 | 文件头为 GGUF，已记录 SHA-256 |
| 演示数据初始化 | 通过 | `python scripts/seed_demo.py` 执行成功 |
| 学生登录与身份隔离 | 通过 | 登录测试通过，演示数据账号和初始账号状态已区分 |
| 前端构建 | 通过 | `npm run build` 执行成功 |
| 应用降级链路 | 已实现 | 模型服务缺失时使用规则兜底，并保留真实测试证据引用 |

## 推荐验收命令

```text
python scripts/seed_demo.py
python -m pytest
cd frontend
npm run build
```

## 当前限制

完整模型推理验证需要可访问的模型服务。当前仅有 GGUF 文件，没有随附的模型服务镜像、启动命令或 ServiceID，因此暂不能在材料中给出在线模型接口验收结果。

模型接入成功后，建议补充：模型服务启动日志、健康检查结果、一次诊断请求和响应、引用校验结果、延迟与资源占用、典型样例评测结果。

